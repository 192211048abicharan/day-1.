#include <stdio.h>

int main() {
    int num, count = 0;

    printf("Enter a number: ");
    scanf("%d", &num);

    count = num == 0 ? 1 : (int)log10(abs(num)) + 1;
    printf("Number of digits in %d: %d\n", num, count);

    return 0;
}
